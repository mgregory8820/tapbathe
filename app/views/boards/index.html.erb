<h2>All Boards</h2>
<%= link_to 'new board', new_board_path %>

<% @boards.each do |board| %>
	<div>
		<h2><%= board.name %></h2>
		<p><%= link_to 'edit', edit_board_path(board) %> | <%= link_to 'Destroy', board, method: :delete, data: { confirm: 'Are you sure?' } %> </p>
	</div>
<% end %>